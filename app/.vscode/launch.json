{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/scripts/index.js",
            "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron"
        }
    ]
}
